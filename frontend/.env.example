# Backend base URL used by the browser and by Next rewrites.
# Local example: http://localhost:3000
# Production example: https://api.hbxsystem.com.br
NEXT_PUBLIC_API_URL=https://api.hbxsystem.com.br

# Google OAuth (login social) — mesmo Client ID configurado no backend GOOGLE_CLIENT_ID
NEXT_PUBLIC_GOOGLE_CLIENT_ID=
# Vazio = o app usa o fallback de lib/app-mobile.ts (/download/android-logistica
# e /download/android, as duas rotas do nginx).
NEXT_PUBLIC_ANDROID_APK_URL=
NEXT_PUBLIC_ANDROID_APK_VENDAS_URL=
