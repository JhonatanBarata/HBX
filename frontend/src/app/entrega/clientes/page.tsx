import { EntregaClientes } from "./page.client";

// A2 — casca da aba "Clientes" do app. A3 preenche (lista/busca/ficha/cadastro).
export default function EntregaClientesPage() {
  return <EntregaClientes />;
}
