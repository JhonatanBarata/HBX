import { EntregaProdutos } from "./page.client";

// A2 — casca da aba "Produtos" do app. A4 preenche (lista + form simples).
export default function EntregaProdutosPage() {
  return <EntregaProdutos />;
}
