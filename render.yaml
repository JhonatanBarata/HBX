services:
  - type: web
    name: hbx-backend
    runtime: node
    rootDir: backend
    buildCommand: npm run render:build
    startCommand: npm run start
    plan: free
    envVars:
      - key: NODE_VERSION
        value: 22
