# Module Rules

Cada modulo deve consultar primeiro as regras globais e depois registrar aqui apenas o que for proprio dele.

Ordem de leitura:
1. `docs/rules/global`
2. `docs/rules/frontend` ou `docs/rules/backend`
3. `docs/rules/modules/<modulo>`
