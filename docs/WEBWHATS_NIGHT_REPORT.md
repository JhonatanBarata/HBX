# WEBWHATS Night Report

Ainda nao executado pelo HBX Night Runner Smart.

Para gerar o relatorio real:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\hbx-night-smart.ps1
```
