Soft rounded chip for attributes, quick facts and toggleable filters.

```jsx
<Chip>Leads locais</Chip>
<Chip active onClick={toggle}>WhatsApp</Chip>
```

Pass `onClick` to make it a toggle; `active` paints the brand-tinted selected state. For non-interactive status pills use `Badge`.
