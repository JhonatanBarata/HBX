/* REFERÊNCIA DE HANDOFF — cópia não-compilada. Ao portar para o repo, restaure o `export`. */
import React from "react";

/**
 * LeadCard — a lead/opportunity tile from the HBX esteira (sales pipeline).
 * Shows the company, recommended channel, priority and next action.
 * `hot` highlights a high-priority lead with a mint spotlight.
 */
function LeadCard({ company, segment, channel = "WhatsApp", priority, nextAction, hot = false, onAction, style, ...rest }) {
  return (
    <article
      style={{
        position: "relative",
        display: "grid",
        gap: "0.6rem",
        padding: "1rem 1.05rem",
        borderRadius: "var(--radius-md)",
        border: "1px solid",
        borderColor: hot ? "color-mix(in srgb, var(--hbx-brand) 36%, var(--hbx-line))" : "var(--border-hairline)",
        background: hot
          ? "radial-gradient(circle at top right, color-mix(in srgb, var(--hbx-brand-strong) 18%, transparent), transparent 42%), linear-gradient(180deg, color-mix(in srgb, var(--hbx-surface) 96%, var(--hbx-background)), color-mix(in srgb, var(--hbx-surface-soft) 92%, var(--hbx-background)))"
          : "linear-gradient(180deg, color-mix(in srgb, var(--hbx-surface) 98%, var(--hbx-background)), color-mix(in srgb, var(--hbx-surface-soft) 92%, var(--hbx-background)))",
        boxShadow: "var(--shadow-inset), var(--shadow-xs)",
        fontFamily: "var(--font-body)",
        ...style,
      }}
      {...rest}
    >
      <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", gap: 8 }}>
        <span style={{ fontFamily: "var(--font-display)", fontSize: "0.6rem", fontWeight: 800, letterSpacing: "0.14em", textTransform: "uppercase", color: hot ? "var(--hbx-brand-strong)" : "var(--hbx-muted)" }}>
          {hot ? "Lead quente" : "Lead"}
        </span>
        {priority != null && (
          <span className="hbx-mono" style={{ fontFamily: "var(--font-mono)", fontVariantNumeric: "tabular-nums", fontSize: "0.7rem", fontWeight: 700, color: "var(--hbx-brand-strong)" }}>
            {priority}% prioridade
          </span>
        )}
      </div>

      <div style={{ display: "grid", gap: 2 }}>
        <strong style={{ fontFamily: "var(--font-display)", fontSize: "1rem", fontWeight: 800, letterSpacing: "-0.02em", color: "var(--text-strong)" }}>{company}</strong>
        {segment && <span style={{ fontSize: "0.76rem", color: "var(--text-muted)" }}>{segment}</span>}
      </div>

      <div style={{ display: "flex", alignItems: "center", gap: 8, flexWrap: "wrap" }}>
        <span style={{ display: "inline-flex", alignItems: "center", gap: 6, padding: "0.3rem 0.55rem", borderRadius: "var(--radius-pill)", border: "1px solid var(--border-hairline)", background: "color-mix(in srgb, var(--hbx-surface-raised) 70%, var(--hbx-surface))", fontSize: "0.72rem", fontWeight: 700, color: "var(--text-body)" }}>
          {channel}
        </span>
        {nextAction && <span style={{ fontSize: "0.74rem", color: "var(--text-muted)" }}>{nextAction}</span>}
      </div>

      {onAction && (
        <button
          type="button"
          onClick={onAction}
          style={{
            marginTop: 4,
            width: "100%",
            minHeight: 40,
            border: "none",
            borderRadius: "var(--control-radius)",
            cursor: "pointer",
            fontFamily: "var(--font-body)",
            fontWeight: 800,
            fontSize: "0.8rem",
            color: "#06121A",
            background: "linear-gradient(135deg, var(--hbx-mint) 0%, #FFFFFF 100%)",
            boxShadow: "0 14px 26px -18px color-mix(in srgb, var(--hbx-mint) 60%, transparent)",
          }}
        >
          Próxima melhor ação: chamar agora
        </button>
      )}
    </article>
  );
}
