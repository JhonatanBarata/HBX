diff --git a/frontend/src/components/mobile/HbxMobileDock.tsx b/frontend/src/components/mobile/HbxMobileDock.tsx
index e92d7a9..d1b8f24 100644
--- a/frontend/src/components/mobile/HbxMobileDock.tsx
+++ b/frontend/src/components/mobile/HbxMobileDock.tsx
@@ -41,6 +41,7 @@ function DockIcon({
     | "sales"
     | "radar"
     | "plus"
+    | "stop"
     | "theme"
     | "more"
     | "support"
@@ -73,6 +74,19 @@ function DockIcon({
       </svg>
     );
   }
+  if (name === "stop") {
+    return (
+      <svg viewBox="0 0 24 24" aria-hidden="true">
+        <defs>
+          <linearGradient id="hbxStopIconGloss" x1="7" y1="5" x2="17" y2="19">
+            <stop offset="0" stopColor="rgba(255,255,255,0.98)" />
+            <stop offset="1" stopColor="rgba(255,255,255,0.84)" />
+          </linearGradient>
+        </defs>
+        <rect x="6.8" y="6.8" width="10.4" height="10.4" rx="2.3" fill="url(#hbxStopIconGloss)" />
+      </svg>
+    );
+  }
   if (name === "theme") {
     return (
       <svg viewBox="0 0 24 24" aria-hidden="true">
@@ -263,9 +277,11 @@ export default function HbxMobileDock({
             onClick={runPrimaryAction}
             aria-label={primaryLabel}
             title={primaryLabel}
+            data-radar-pulse={primaryTone === "danger" ? "true" : "false"}
           >
-            <DockIcon name="plus" />
+            <DockIcon name={primaryTone === "danger" ? "stop" : "plus"} />
           </button>

           <button
diff --git a/frontend/src/app/mobile-system.css b/frontend/src/app/mobile-system.css
index 5aaae59..a6c09e1 100644
--- a/frontend/src/app/mobile-system.css
+++ b/frontend/src/app/mobile-system.css
@@ -437,6 +437,7 @@

   .hbx-mobile-dock-primary {
     width: 58px;
+    position: relative;
     height: 58px;
     display: grid;
     place-items: center;
@@ -457,6 +458,16 @@
       filter var(--hbx-mobile-spring);
   }

+  .hbx-mobile-dock-primary::before,
+  .hbx-mobile-dock-primary::after {
+    content: "";
+    position: absolute;
+    inset: -12px;
+    z-index: -1;
+    border-radius: inherit;
+    pointer-events: none;
+  }
+
   .hbx-mobile-dock-primary svg {
     width: 26px;
     height: 26px;
@@ -467,13 +478,60 @@
     background:
       linear-gradient(180deg, rgba(255, 255, 255, 0.2), transparent 48%),
       linear-gradient(135deg, #b91c1c 0%, #ef4444 58%, #fb7185 100%);
+    filter: saturate(1.08);
     box-shadow:
       inset 0 1px 0 rgba(255, 255, 255, 0.28),
-      0 22px 42px -22px rgba(185, 28, 28, 0.94);
+      inset 0 -8px 14px rgba(127, 29, 29, 0.16),
+      0 22px 42px -22px rgba(185, 28, 28, 0.94),
+      0 0 0 1px rgba(255, 255, 255, 0.18);
+  }
+
+  .hbx-mobile-dock-primary[data-tone="danger"]::before {
+    background:
+      radial-gradient(circle, rgba(239, 68, 68, 0.34) 0 38%, rgba(239, 68, 68, 0.14) 39% 56%, transparent 57%),
+      radial-gradient(circle, rgba(248, 113, 113, 0.18) 0 58%, transparent 59%);
+    animation: hbxMobileRadarStopPulse 1.55s ease-out infinite;
+  }
+
+  .hbx-mobile-dock-primary[data-tone="danger"]::after {
+    inset: -22px;
+    border: 1px solid rgba(239, 68, 68, 0.2);
+    background:
+      radial-gradient(circle, transparent 0 46%, rgba(248, 113, 113, 0.2) 47% 48%, transparent 49% 100%);
+    animation: hbxMobileRadarStopWave 1.55s ease-out infinite;
+  }
+
+  .hbx-mobile-dock-primary[data-tone="danger"] svg {
+    width: 25px;
+    height: 25px;
+    filter: drop-shadow(0 1px 0 rgba(127, 29, 29, 0.22));
+  }
+
+  .hbx-mobile-dock-primary[data-tone="danger"] svg rect {
+    stroke: rgba(255, 255, 255, 0.86);
+    stroke-width: 0.45;
   }

   .hbx-mobile-dock-item:active {
     transform: scale(0.95);
   }

+  .hbx-mobile-dock-primary[data-radar-pulse="true"] {
+    animation: hbxMobileRadarStopBreath 1.55s ease-in-out infinite;
+  }
+
   .hbx-mobile-dock-primary:active {
     transform: translateY(-6px) scale(0.94);
     box-shadow:
@@ -754,6 +812,36 @@
     }
   }

+  @keyframes hbxMobileRadarStopBreath {
+    0%, 100% {
+      transform: translateY(-8px) scale(1);
+    }
+
+    50% {
+      transform: translateY(-8px) scale(1.045);
+    }
+  }
+
+  @keyframes hbxMobileRadarStopPulse {
+    0% {
+      opacity: 0.62;
+      transform: scale(0.72);
+    }
+
+    100% {
+      opacity: 0;
+      transform: scale(1.42);
+    }
+  }
+
+  @keyframes hbxMobileRadarStopWave {
+    0% {
+      opacity: 0.36;
+      transform: scale(0.76);
+    }
+
+    100% {
+      opacity: 0;
+      transform: scale(1.36);
+    }
+  }
+
   @keyframes hbxMobileSheetBackdropIn {
     from {
       opacity: 0;
