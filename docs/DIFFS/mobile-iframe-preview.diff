Subject: mobile: preview mobile routes with iframe viewport

diff --git a/frontend/src/app/mobile/layout.tsx b/frontend/src/app/mobile/layout.tsx
--- a/frontend/src/app/mobile/layout.tsx
+++ b/frontend/src/app/mobile/layout.tsx
@@ -1,13 +1,9 @@
 import type { ReactNode } from "react";
 
-import styles from "./MobileViewportShell.module.css";
+import MobileViewportFrame from "./MobileViewportFrame";
 
 export default function MobileLayout({ children }: { children: ReactNode }) {
   return (
-    <div className={styles.mobilePreviewPage}>
-      <main className={styles.mobileViewport} aria-label="HBX mobile">
-        {children}
-      </main>
-    </div>
+    <MobileViewportFrame>{children}</MobileViewportFrame>
   );
 }
diff --git a/frontend/src/app/mobile/MobileViewportFrame.tsx b/frontend/src/app/mobile/MobileViewportFrame.tsx
new file mode 100644
--- /dev/null
+++ b/frontend/src/app/mobile/MobileViewportFrame.tsx
@@ -0,0 +1,77 @@
+"use client";
+
+import { type ReactNode, useEffect, useState } from "react";
+
+import styles from "./MobileViewportShell.module.css";
+
+const FRAME_PARAM = "hbxMobileFrame";
+const DESKTOP_QUERY = "(min-width: 768px)";
+
+type FrameState = {
+  ready: boolean;
+  isDesktop: boolean;
+  isFramed: boolean;
+  frameSrc: string;
+};
+
+function buildFrameState(): FrameState {
+  if (typeof window === "undefined") {
+    return {
+      ready: false,
+      isDesktop: false,
+      isFramed: false,
+      frameSrc: "",
+    };
+  }
+
+  const url = new URL(window.location.href);
+  const isFramed = url.searchParams.get(FRAME_PARAM) === "1";
+  const isDesktop = window.matchMedia(DESKTOP_QUERY).matches;
+
+  url.searchParams.set(FRAME_PARAM, "1");
+
+  return {
+    ready: true,
+    isDesktop,
+    isFramed,
+    frameSrc: `${url.pathname}${url.search}${url.hash}`,
+  };
+}
+
+export default function MobileViewportFrame({ children }: { children: ReactNode }) {
+  const [frameState, setFrameState] = useState<FrameState>(() => buildFrameState());
+
+  useEffect(() => {
+    const syncFrameState = () => {
+      setFrameState(buildFrameState());
+    };
+
+    syncFrameState();
+
+    const mediaQuery = window.matchMedia(DESKTOP_QUERY);
+    mediaQuery.addEventListener("change", syncFrameState);
+    window.addEventListener("popstate", syncFrameState);
+
+    return () => {
+      mediaQuery.removeEventListener("change", syncFrameState);
+      window.removeEventListener("popstate", syncFrameState);
+    };
+  }, []);
+
+  if (frameState.ready && frameState.isDesktop && !frameState.isFramed) {
+    return (
+      <div className={styles.mobilePreviewPage} data-frame="true">
+        <iframe
+          className={styles.mobilePreviewFrame}
+          src={frameState.frameSrc}
+          title="HBX mobile preview"
+        />
+      </div>
+    );
+  }
+
+  return (
+    <div className={styles.mobilePreviewPage} data-frame="false">
+      <main className={styles.mobileViewport} aria-label="HBX mobile">
+        {children}
+      </main>
+    </div>
+  );
+}
diff --git a/frontend/src/app/mobile/MobileViewportShell.module.css b/frontend/src/app/mobile/MobileViewportShell.module.css
--- a/frontend/src/app/mobile/MobileViewportShell.module.css
+++ b/frontend/src/app/mobile/MobileViewportShell.module.css
@@ -1,59 +1,89 @@
 .mobilePreviewPage {
   min-height: 100dvh;
   background:
     radial-gradient(circle at 0% 0%, color-mix(in srgb, var(--brand) 12%, transparent), transparent 420px),
     radial-gradient(circle at 100% 0%, color-mix(in srgb, var(--brand-solid) 10%, transparent), transparent 380px),
     linear-gradient(180deg, var(--background) 0%, var(--background-alt) 100%);
 }
 
 .mobileViewport {
   width: 100%;
   min-height: 100dvh;
   background: var(--background);
 }
+
+.mobilePreviewFrame {
+  display: block;
+  width: 100%;
+  height: 100%;
+  border: 0;
+  background: var(--background);
+}
 
 @media (min-width: 768px) {
   .mobilePreviewPage {
     display: grid;
     place-items: start center;
     padding: 18px;
-    overflow: hidden;
+    overflow: auto;
   }
 
-  .mobileViewport {
-    width: min(430px, calc(100vw - 36px));
-    min-height: 0;
-    height: calc(100dvh - 36px);
-    max-height: 932px;
+  .mobilePreviewPage[data-frame="true"] {
+    min-height: 100dvh;
+  }
+
+  .mobilePreviewPage[data-frame="false"] {
+    display: block;
+    padding: 0;
+    overflow: visible;
+    background: var(--background);
+  }
+
+  .mobilePreviewPage[data-frame="true"] .mobilePreviewFrame {
+    width: min(430px, calc(100vw - 36px));
+    height: min(932px, calc(100dvh - 36px));
     overflow: auto;
     overscroll-behavior: contain;
     border: 1px solid color-mix(in srgb, var(--line) 82%, transparent);
     border-radius: 34px;
     background: var(--background);
     box-shadow: 0 24px 80px color-mix(in srgb, var(--foreground) 16%, transparent);
   }
 
-  .mobileViewport::-webkit-scrollbar {
+  .mobilePreviewPage[data-frame="true"] .mobilePreviewFrame::-webkit-scrollbar {
     width: 0;
     height: 0;
   }
+
+  .mobileViewport {
+    width: 100%;
+    min-height: 100dvh;
+    background: var(--background);
+  }
 }
 
 :global(html[data-theme-mode="dark"]) .mobilePreviewPage {
   background:
     radial-gradient(circle at 0% 0%, color-mix(in srgb, var(--brand) 14%, transparent), transparent 420px),
     radial-gradient(circle at 100% 0%, color-mix(in srgb, var(--brand-solid) 13%, transparent), transparent 380px),
     linear-gradient(180deg, var(--background) 0%, var(--background-alt) 100%);
 }
 
 :global(html[data-theme-mode="dark"]) .mobileViewport {
   background: var(--background);
+}
+
+:global(html[data-theme-mode="dark"]) .mobilePreviewFrame {
+  background: var(--background);
+}
+
+@media (min-width: 768px) {
+  :global(html[data-theme-mode="dark"]) .mobilePreviewPage[data-frame="false"] {
+    background: var(--background);
+  }
+
+  :global(html[data-theme-mode="dark"]) .mobilePreviewPage[data-frame="true"] .mobilePreviewFrame {
+    background: var(--background);
+    border-color: color-mix(in srgb, var(--line) 88%, transparent);
+    box-shadow: 0 24px 90px color-mix(in srgb, var(--foreground) 10%, transparent);
+  }
+}
-
-:global(html[data-theme-mode="dark"]) .mobileViewport {
-  background: var(--background);
-  border-color: color-mix(in srgb, var(--line) 88%, transparent);
-  box-shadow: 0 24px 90px color-mix(in srgb, var(--foreground) 10%, transparent);
-}
