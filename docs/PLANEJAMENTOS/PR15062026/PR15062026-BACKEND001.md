# PR15062026 — Fila de backend (anotado, NÃO aplicado)

> Ordem do dono 15/06: anotar, não implementar agora. Quando for mexer no backend.

## 1. E-mail de signup usa o e-mail na saudação (feio) → usar o NOME

**Sintoma:** o e-mail enviado no cadastro abre com `olá {email}` (o endereço de
e-mail). Fica ruim.

**Correção (mínima, só o e-mail que envia — NÃO mexer no fluxo):** a saudação do
template de signup/confirmação deve usar o campo **"Como deseja ser chamado?"**
(`name` do `/auth/signup`), não o `email`. Trocar só o render do template
(provavelmente no módulo de mail / template de `email_confirmation` /
`seller_welcome`) pra interpolar o `name`.

**Futuro (idéia do dono, NÃO agora):** "Como deseja ser chamado?" pode virar o
**login** — pra quem não quiser entrar pelo e-mail. Só anotado; decidir depois.

Arquivos prováveis (conferir antes): `backend/src/auth` (signup), módulo de e-mail
por empresa / Master mailer, templates `email_confirmation`/`seller_welcome`.
