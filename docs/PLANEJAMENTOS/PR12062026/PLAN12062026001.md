# PLAN12062026001 — Fila de pequenas edições de BACKEND (aplicar em lote)

> Combinado com o dono em 12/06/2026: pequenas edições de backend que ele
> avisar NÃO são aplicadas na hora — entram aqui como itens numerados e são
> aplicadas TODAS DE UMA VEZ quando ele mandar ("aplica a fila").
> Frontend não passa por aqui: edita na hora (dev).

## Como aplicar a fila (no dia da aplicação)

1. Implementar os itens na ordem, no código do backend.
2. `cd backend && npm run prisma:validate && npm run build`.
3. `docker restart backend` — **obrigatório**: o `start:dev` do container
   não recebe eventos de arquivo do mount do Windows; mudança de backend só
   pega com restart.
4. Validar item a item (curl/tela) e marcar como APLICADO aqui.

## Fila

### E1 — Vitrine pública do catálogo de planos
- **Pedido (dono):** página /planos pública e coerente para campanha de Google
  Ads (entrada trial-first, 12/06/2026).
- **Onde:** `backend/src/commercial-plans` (controller).
- **Mudança:** expor um `GET` SEM auth (ex.: `/commercial-plans/public-catalog`)
  devolvendo só a vitrine: key, title, monthlyPrice, headline, badge,
  trialDays, features (catálogo `buildCommercialPlansCatalog()` sem dados de
  empresa/usuário). O front (/planos) já consome o formato `{ plans: [...] }`
  e hoje só mostra preço para visitante logado.
- **Risco/observação:** dados já são de marketing público; nenhum dado de
  tenant na resposta. Rate-limit básico recomendado.
- **Status:** APLICADO (12/06/2026) — `GET /commercial-plans/public-catalog`
  (controller público + throttle 30/min); /planos consome com fallback no
  catálogo autenticado. Validado: visitante deslogado vê R$ 45/R$ 99 reais.

### E2 — E-mail de vendedor vazando para cadastro de cliente
- **Pedido (dono):** cadastro novo de cliente recebeu o e-mail "bem-vindo à
  equipe comercial" com senha temporária — era exclusivo dos vendedores.
- **Investigação (12/06/2026):**
  - O destinatário (glaucoreserva91@gmail.com) é ADMIN da empresa nova
    "HBX3" — cadastro de CLIENTE, não vendedor.
  - Links do e-mail apontam para `https://www.hbxsystem.com.br` → quem
    enviou foi o backend de PRODUÇÃO (VPS, build antigo), não o local.
  - No código atual, o único despachante do template `seller_welcome` é a
    ativação de onboarding de vendedor (senha `Hbx@...`); o prefixo
    `Tmp@...` vem do reset de senha do master (build antigo enviava o
    welcome de vendedor nesses fluxos).
- **Mudança aplicada (local):** trava estrutural em
  `sendWelcomeAccessEmail` — `seller_welcome` NUNCA é enviado para role
  diferente de USER (loga warning e não envia). Cliente recebe apenas a
  confirmação de e-mail/trial.
- **Status:** APLICADO no local (12/06/2026). **PENDENTE: deploy do backend
  atualizado no VPS** — até lá, produção continua com o build antigo que
  vaza o e-mail. Deploy é ação do dono (INFRA.md).

<!-- Modelo de item:
### E1 — [título curto]
- **Pedido (dono):** [o que ele falou, literal]
- **Onde:** [arquivo/serviço/endpoint]
- **Mudança:** [descrição objetiva da edição]
- **Risco/observação:** [se houver]
- **Status:** pendente | APLICADO (data)
-->

### E3 — Produto nasce com preço 0 quando criado com `price`
- **Onde:** `products.service.ts` → `normalizePriceCents` + `hasOwn`.
- **Causa:** o ValidationPipe entrega o DTO com `priceCents` como propriedade
  própria `undefined`; o `hasOwn(dto,'priceCents')` é true e ignora o `price`
  enviado → produto criado com 0. Workaround atual: enviar `priceCents`.
- **Mudança:** tratar `undefined` como ausente (checar `!== undefined` em vez
  de `hasOwn`) nos normalizadores de preço/desconto/comissão.
- **Status:** APLICADO (12/06/2026 — criar com price:77 → R$77; testes 11/11 verdes)

### E4 — PATCH /products/:id retorna 500 (versionamento)
- **Onde:** `products.service.ts:268` → `this.pv.createVersion(...)`.
- **Causa:** Prisma "Expected String, provided Object" — o snapshot do
  produto vai como objeto para um campo String do ProductVersion (falta
  `JSON.stringify`).
- **Mudança:** serializar o snapshot no createVersion.
- **Status:** APLICADO (12/06/2026 — PATCH/arquivar 200; edição validada na tela do gerencial)

### E5 — Caso de cancelamento (reversão de comissão SÓ com decisão do master)
- **Pedido (dono, 12/06/2026):** todo cancelamento avisa master E vendedor;
  comissão nunca estorna sozinha; com saldo a restituir, o master triagem
  primeiro e o vendedor só é avisado no desfecho; master pode manter,
  estornar ou desvincular a venda do vendedor.
- **Desenho aprovado ("caso de cancelamento"):**
  1. Cancelamento (saleStatus → canceled/inactive) ABRE UM CASO com campo
     calculado `saldoARestituir` (anual: meses não usados; mensal: 0).
  2. **Remover o estorno automático atual** (`endsSale → commissionStatus
     'canceled'` em buildSaleCommissionPatch): no cancelamento, comissões
     recorrentes futuras desligam e o pendente/payable fica TRAVADO pelo
     caso aberto (gerador de payout ignora leads com caso aberto). `paid`
     fica pago (estorno de payout segue manual).
  3. Avisos: master SEMPRE e na hora (master-notice; fila filtrável por
     saldoARestituir > 0). Vendedor: saldo 0 → avisado no mesmo evento
     (texto FACTUAL: "cliente cancelou; futuras param; pendente em
     revisão"); saldo > 0 → avisado só quando o master finalizar.
  4. Decisão do master fecha o caso: manter (libera payable), estornar
     (canceled), desvincular venda do vendedor (verificar se o PATCH do
     gerencial cobre; senão, incluir ação própria). Desfecho gera o
     segundo aviso ao vendedor ("mantida"/"estornada").
- **Onde:** vendas.service (buildSaleCommissionPatch + geração de payout),
  modelo novo do caso (ou campos no lead), hooks de notice, ações gerencial.
- **Risco/observação:** maior item da fila — tocar com testes dos fluxos de
  comissão existentes; nenhuma mudança nos status atuais da comissão.
- **Status:** APLICADO (12/06/2026). Validado E2E ponta a ponta:
  cancelamento manteve a comissão (pending, sem estorno), abriu o caso
  (refund 0 no mensal), avisou o sino do admin ("Cancelamento: Padaria do
  Teste"), travou o payout, e a decisão "Manter" pela tela do Gerencial
  resolveu o caso preservando a comissão. Testes de comissão 68/68 verdes
  antes e depois. Implementação: colunas via schema-runtime-ensure
  (vendas-cancellation-case-columns), buildSaleCommissionPatch sem estorno
  automático, notice audiência customer, timeline factual ao vendedor,
  GET /vendas/cancellation-cases + PATCH /vendas/lead/:id/cancellation-case
  (kept|reversed|unlinked), e a seção "Casos de cancelamento — sua triagem"
  na aba Comissões do /gerencial com o filtro "só com saldo a restituir".

## Já aplicado fora da fila (referência)

- 12/06/2026 — Login com identificador caixa-insensível
  (`findByLoginIdentifier` em users.service; aplicado em login, passport e
  conflito de cadastro; senha estrita). Aprovado e validado no backend real
  antes deste combinado existir.

## E6 — Encoding quebrado nos textos do motor (mixin admin)

- **O que:** strings pt-BR com mojibake NO FONTE de
  `backend/src/webscraping/radar/01-search/radar-core-factory-admin.mixin.ts`
  (diagnósticos: "nÃ£o", "produÃ§Ã£o", "FÃ¡brica", "memÃ³ria"…). O texto chega
  quebrado na janela Motor do /master (visto na validação de 12/06/2026,
  PR12062026003-MASTER). Corrigir o encoding das strings (UTF-8 real) —
  conferir se outros arquivos do radar têm o mesmo problema.
- **Onde:** radar-core-factory-admin.mixin.ts (e possivelmente vizinhos).
- **Risco/observação:** zero lógica — só literais de string. Build + restart.
- **Status:** APLICADO (12/06/2026). 241 linhas corrigidas em 10 arquivos do
  radar (conversão cp1252→UTF-8 linha a linha). Exceção PROPOSITAL mantida:
  radar-core-campaign-planner.mixin.ts:864 compara input com 'econÃ´mico'
  de propósito (aceita config antiga gravada com mojibake — corrigir mudaria
  comportamento). Validado no /master: diagnósticos do motor 100% limpos.

## E7 — Endpoint espelho master para tickets (owner/tickets)

- **O que:** a janela Tickets do /master não pode chamar `owner/tickets`
  (exige x-owner-secret de servidor, canal M2M do Ops Control). Criar
  espelho de leitura com JWT + MasterGuard (ex.: GET modules/master/tickets
  → tickets.listOwnerTickets) para a janela listar hbx_support_ticket/hbx_job.
- **Onde:** owner/owner-tickets.controller.ts (novo controller ou rota irmã).
- **Risco/observação:** leitura apenas; dispatch continua M2M.
- **Status:** APLICADO (12/06/2026). MasterTicketsController novo
  (GET modules/master/tickets, JWT+MasterGuard) registrado no OwnerModule;
  janela Tickets do /master ligada nele (filtro por status new/triage/
  waiting_codex/in_progress/done + detalhe com jobs). Validado: 200
  {ok, tickets:[]} no dev (sem tickets gravados ainda) e UI listando.

## E8 — Histórico de notificações de pagamento (master/payment-notifications)

- **O que:** o POST /mercadopago-approved (webhook M2M) agora grava histórico
  best-effort (falha de log NUNCA bloqueia o disparo) na tabela nova
  MasterPaymentNotificationLog (model no schema + runtime-ensure
  ensureMasterPaymentNotificationLogTable nos 3 lugares do PrismaService,
  shouldBecomeMigration=true). Novo GET /master/payment-notifications/history
  (JWT + MasterGuard, filtros status/companyId/take) alimenta a janela
  Pagamentos do /master.
- **Onde:** prisma/schema.prisma, prisma.service.ts,
  messaging/master-payment-notifications.controller.ts.
- **Risco/observação:** gravação em try/catch; leitura devolve mensagem
  honesta se a tabela não existir no ambiente.
- **Status:** APLICADO (12/06/2026, autorização total do dono). Validado:
  ensure rodou no boot (durationMs=83), GET 200 com lista vazia e a janela
  Pagamentos renderizando filtros + estado vazio honesto.

## E9 — SMTP por empresa (pendência item 7 do PENDENCIAS-FRONTEND)

- **Pedido (dono):** o admin precisa de configuração de e-mail (SMTP). A
  empresa **HBX** usa o **SMTP do master (compartilhado — a única
  compartilhada)**; as **demais empresas** podem **adicionar o próprio SMTP**
  se quiserem (opcional).
- **Estado atual (auditado 12/06):** `backend/src/mail/mail.service.ts` monta
  UM transport GLOBAL a partir de ENV (`SMTP_HOST`/`SMTP_PORT`/`SMTP_USER`/
  `SMTP_PASS`/`MAIL_FROM`/`MAIL_FROM_NAME`… via `readEnv`) — sem noção de
  tenant. `mail/master-email.controller.ts` edita só o remetente do master.
- **Mudança proposta:**
  1. **Schema:** campos SMTP por `Company` (smtpHost/smtpPort/smtpSecure/
     smtpUser/smtpPassEnc/mailFrom/mailFromName), segredo **cifrado** (mesmo
     padrão das credenciais master). runtime-ensure (shouldBecomeMigration).
  2. **mail.service:** resolver o transport por `companyId` → empresa com SMTP
     próprio usa o dela; senão cai no ENV global (HBX/master = compartilhado).
     Propagar `companyId` nos pontos de envio por tenant.
  3. **Front:** aba "E-mail/SMTP" em Configurações (admin) para preencher +
     **Testar** (POST test por empresa). HBX segue no compartilhado.
- **Risco/observação:** toca **credenciais/secrets e infra de e-mail** →
  exige **ordem explícita do dono** (CLAUDE.md: secrets). Cifrar a senha,
  nunca logar segredo, testar com a conta de teste antes. Maior item da fila.
- **Status:** PENDENTE (fila) — não aplicar ao vivo.

## E10 — Módulo de onboarding do vendedor nasce DESLIGADO (pendência item 9)

- **Pedido (dono):** o fluxo rico de vendedor (documentação, contrato, e-mails
  **Onboarding** + **Boas-vindas**) é um **módulo que nasce OFF** para os
  admins; o admin **ativa em Configurações**; se não ativar, as abas/e-mails
  dele **não aparecem**.
- **Estado atual (auditado 12/06):** `seller_welcome` e
  `seller_onboarding_request` são defaults em
  `mail/email-template.service.ts` e aparecem SEMPRE no editor master;
  `gerencial/seller-onboarding.service.ts` roda o fluxo. **Não há flag de
  módulo por empresa.**
- **Mudança proposta:**
  1. **Flag por `Company`** (ex.: `sellerOnboardingEnabled`, default
     **false**) — runtime-ensure.
  2. **Gating:** abas/ações de onboarding (gerencial) e a visibilidade dos
     templates `seller_*` condicionadas à flag; **toggle em Configurações**
     (admin). Se OFF, nada do módulo aparece.
- **Verificado (corrige o doc):** o "typo `herança\indicação` (barra
  sobrando)" relatado **NÃO existe** no código atual — o default lê
  `herança/indicação` (barra normal, correto) em
  `email-template.service.ts:211`. Nada a consertar aí.
- **Risco/observação:** gating de UI + flag; **sem** mudança no caminho do
  dinheiro.
- **Status:** PENDENTE (fila) — não aplicar ao vivo.

### E11 — Comissão do vendedor exposta no /profile/current-user (tela /leads)
- **Pedido (dono, 13/06/2026):** na tela /leads, "se for vendedor a comissão
  dele seria legal ter nos botões também".
- **Onde:** `backend/src/auth/profile.controller.ts` → `sanitizeUser`.
- **Mudança (JÁ no código):** `sellerProfile.commissionPercent` passa a sair no
  payload (lê `user.commissionPercent`, que o `findById` já traz via `include`).
  É a comissão de VENDA do próprio vendedor — compensação dele, NÃO é
  cobrança/plano/preço da empresa (regra PR-002 D.4 continua intacta: nenhum
  campo de billing novo). Frontend (`leads/page.client.tsx`) já consome com
  fallback silencioso (só mostra se `userKind=seller` e percentual > 0).
- **Risco/observação:** aditivo, leitura apenas, zero caminho de dinheiro novo.
  Testes `auth.service.test.ts` (sanitizeUser) não assertam a forma do
  `sellerProfile`, então não quebram. `npm run build` OK.
- **Status:** APLICADO (13/06/2026). `docker restart backend` feito (container
  `backend`, boot limpo "Nest application successfully started", port 3000,
  `/profile/current-user` 401 sem token = vivo). Testes: auth.service 14/14
  (vazamento de cobrança intacto) e webscraping 120/120. Badge de comissão
  acende para vendedor com `commissionPercent > 0`.

### E12 — KPIs reais da tela /atendimento (tempo médio de resposta + conversões)
- **Pedido (dono, 13/06/2026):** "segue" — ligar os 2 KPIs que sobravam em "—"
  na tela /atendimento ("Tempo médio de resposta" e "Conversões").
- **Onde:** `inbox.controller.ts` (`GET /inbox/metrics`, sob
  `@ModuleAccess('atendimento')`) + `inbox.service.ts` (`getInboxMetrics`).
- **Mudança (JÁ no código):** endpoint de leitura agregada, SEM schema novo:
  - `avgResponseSeconds`: tempo médio de 1ª resposta dos últimos 7 dias —
    agrupa `CompanyMessage` por conversa e mede do primeiro INBOUND não
    respondido até o próximo OUTBOUND (take 8000 msgs, gap ≤ 7d). `null`
    quando não há episódios → front mostra "—".
  - `conversions`: `prisma.vendasLead.count({ saleStatus: 'sale_confirmed' })`
    da empresa (MESMA definição de venda paga do Vendas). `try/catch` → 0.
  - Frontend (`atendimento/page.client.tsx`) já consome com fallback
    silencioso (`loadMetrics`; KPIs ficam "—" enquanto o endpoint não sobe).
- **Risco/observação:** aditivo, leitura apenas, isolado por `companyId`, zero
  caminho de dinheiro/secret/migration. `prisma:validate` + `build` (tsc)
  verdes; frontend lint(arquivo)+check-pele(590/590)+build verdes.
- **Status:** IMPLEMENTADO no código; PENDENTE só `docker restart backend`.
  Validar pós-restart: `GET /inbox/metrics` → `{windowDays:7,
  avgResponseSeconds, responseEpisodes, conversions}` e os 2 KPIs acendendo.

### E? — Mensagens rápidas do Atendimento (tabela nova) — JÁ IMPLEMENTADO, falta APLICAR
- **Pedido (dono, 12/06/2026):** botão "Inserir mensagem rápida" da tela
  /atendimento precisa funcionar; decisão dele = **tabela nova no schema**
  (não bot-config, não localStorage).
- **Onde / o que já está no código (NÃO aplicado ainda):**
  - `prisma/schema.prisma`: model `AtendimentoQuickReply` (companyId escalar,
    sem back-relation; title, content, sortOrder, createdByUserId?, timestamps).
  - `prisma/migrations/20260612_atendimento_quick_reply/migration.sql`
    (CREATE TABLE aditivo + 2 índices).
  - `inbox.service.ts`: `listQuickReplies` / `createQuickReply` / `deleteQuickReply`.
  - `inbox.controller.ts`: `GET /inbox/quick-replies`, `POST /inbox/quick-replies`,
    `DELETE /inbox/quick-replies/:id` (sob `@ModuleAccess('atendimento')`).
  - Frontend já consome (com fallback silencioso enquanto o endpoint não sobe).
- **Aplicar:** `cd backend && npm run prisma:validate && npm run build` (OK) →
  `npx prisma migrate deploy` (cria a tabela) → `docker restart backend`.
- **Risco/observação:** aditivo e isolado por empresa; sem caminho de dinheiro,
  sem dado de tenant cruzado. `npm run build` e `prisma validate` já passaram.
- **Status:** IMPLEMENTADO no código; PENDENTE só o `migrate deploy` + restart.

### E13 — Admin é singular/pagante: só o Master cunha/altera admin + admin não gerencia o próprio acesso
- **Pedido (dono, 13/06/2026, decisões confirmadas):**
  - "admin nunca gerencia seus próprios acessos, quem controla o que o admin
    vê é o MASTER";
  - "admin não transfere o seu admin pra outra pessoa, ele é quem paga" →
    **só o Master cria/altera administrador**.
  - Escopo escolhido: **front + backend** (front já aplicado; backend nesta fila).
- **Front já aplicado (13/06, dev):**
  - `novo-acesso-modal.tsx`: removido o seletor Vendedor/Admin; admin só cria
    VENDEDOR (role travado em USER).
  - `gerenciar-vendedor-modal.tsx`: aba "Acessos" só para alvo VENDEDOR; removido
    o botão "Tornar Admin/Vendas"; self já estava travado.
- **Backend a aplicar (todas em `users/users.controller.ts` + 1 em `team/`):**
  1. `team/team-policy.service.ts` → `assertCanManage`: depois do
     `if (requester?.isSystemMaster) return;`, adicionar — se
     `normalizeRole(target?.role) === 'ADMIN'` → `ForbiddenException('Acesso do
     administrador é controlado pelo Master.')`. (Cobre self e qualquer admin;
     Master segue editando; VENDEDOR continua editável pelo admin.) **Leitura
     intacta** (`assertCanRead` mantém self-read).
  2. `updateRole` (`PATCH :id/role`, `@Admin`): bloquear
     `Number(req.user.id) === id` (self) E qualquer mudança envolvendo ADMIN no
     contexto da empresa — se `role === 'ADMIN'` OU `target.role === 'ADMIN'`
     → `ForbiddenException('Só o Master cria ou altera administradores.')`.
     (Master usa `PATCH /users/master/:id` — intacto.)
  3. `deleteCompanyUser` (`DELETE :id/delete`, `@Admin`): bloquear
     `Number(req.user.id) === id` → `ForbiddenException('Você não pode excluir o
     próprio acesso.')`.
  4. `createCompanyUser` (`POST company/create`, `@Admin`): se
     `dto.role === 'ADMIN'` → `ForbiddenException('Só o Master cria
     administradores.')`; forçar `role = 'USER'`. (O check de trial `maxAdmins`
     fica inócuo, pode manter.)
- **Risco/observação:** mexe em **AUTORIZAÇÃO** (regra de segurança CLAUDE.md) —
  **autorizado pelo dono em 13/06/2026** (front+backend). Caminho do Master
  (`/users/master/*`, `master/company/:id/create`) **intacto**. Sem schema,
  sem migration, sem caminho de dinheiro. Testes: `team-policy.service.test.ts`
  edita política de VENDEDOR (alvo 7) — segue verde; conferir no apply que
  nenhum teste edita política de **alvo admin** como não-master. `build` +
  `docker restart backend` obrigatório.
- **Status:** PENDENTE (fila) — não aplicar ao vivo.

### E14 — Papel Gerente (C3 da Régua única de acesso): Admin-sem-cobrança, criável pelo dono
- **Pedido (dono, 13/06/2026, decisões confirmadas):** existir um "gerente" =
  todos os acessos operacionais do admin, **MENOS ver Cobrança/plano (valores
  da empresa)**. Decisões: (a) **o DONO também cria** o gerente (não só o
  Master); (b) o **único** acesso retirado = **Cobrança/plano** — preço,
  comissão e cancelamento **CONTINUAM** com o gerente (são toggles já
  enforced em `team-access-runtime`, dá pra tirar depois se ele quiser).
- **Representação (estender, NÃO remontar — sem enum de papel novo):** `role`
  segue `ADMIN`; o que separa dono × gerente é um marcador de "dono-pagante".
  - **Recomendado:** chave de política nova `commercial.billing.view` no
    catálogo (grupo `admin`, `defaultForAdmin:true`, `riskLevel:'high'`,
    `backendEnforced:true`) — **sem migration**, reusa a máquina do
    `team-access-catalog`/policy/Acessos.
  - Alternativa: coluna boolean `User.isBillingOwner` (custa migration).
  - **Label derivado no front** (sem papel novo): ADMIN com billing ON →
    "Administrador"; ADMIN com billing OFF → "Gerente".
- **Onde / mudanças:**
  1. `auth/profile.controller.ts:48` `billingAudience`: trocar `role==='ADMIN'`
     por `isSystemMaster || temBillingView`. Expor no payload um flag
     (ex.: `sellerProfile.canViewBilling`) pro front saber esconder a seção.
  2. `financeiro/financeiro.service.ts:669` `canManageBilling`: idem
     (`isSystemMaster || temBillingView`); o `assertCanManageBilling` segue
     valendo em todos os endpoints de cobrança.
  3. `commercial-plans` (GET `/commercial-plans/me`, `permissions.canSelectPlan`):
     alinhar a visibilidade/veredito ao mesmo gate de billing.
  4. Catálogo: `commercial.billing.view` + enforcement nos pontos de cobrança
     (ou resolver tudo via `canManageBilling = temBillingView`).
  5. **Criação pelo dono** (amenda o **E13 item 4**): em
     `users.controller.ts createCompanyUser`, o admin-dono pode criar
     `role=USER` (vendedor) **e** `role=ADMIN` com `billing.view=false`
     (gerente). Admin **com** cobrança (billing.view true) segue **só Master**.
     `updateRole` e edição de política de admin continuam Master-only (E13) →
     gerente **não liga a própria cobrança**.
  6. **Front:** a seção "Plano e cobrança" em `configuracoes/page.client.tsx`
     passa a esconder por `!canViewBilling` (hoje esconde por
     `userKind==='seller'` — gerente é ADMIN e hoje veria a seção vazia).
- **Invariante:** master enxerga TUDO (o `isSystemMaster ||` permanece em todos
  os gates).
- **Risco/observação:** mexe em **AUTORIZAÇÃO + visibilidade de valores** —
  C3 da régua, autorizado 13/06. Sem caminho de dinheiro novo; **sem migration**
  na opção recomendada. Testes: `profile.controller`/sanitize (vazamento de
  billing) e guard do `financeiro`. Front: lint(check-pele)+build. `docker
  restart backend`.
- **Status:** PENDENTE (fila) — não aplicar ao vivo.
