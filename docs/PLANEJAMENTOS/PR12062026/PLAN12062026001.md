# PLAN12062026001 — Fila de pequenas edições de BACKEND (aplicar em lote)

> Combinado com o dono em 12/06/2026: pequenas edições de backend que ele
> avisar NÃO são aplicadas na hora — entram aqui como itens numerados e são
> aplicadas TODAS DE UMA VEZ quando ele mandar ("aplica a fila").
> Frontend não passa por aqui: edita na hora (dev).

## Como aplicar a fila (no dia da aplicação)

1. Implementar os itens na ordem, no código do backend.
2. `cd backend && npm run prisma:validate && npm run build`.
3. `docker restart backend` — **obrigatório**: o `start:dev` do container
   não recebe eventos de arquivo do mount do Windows; mudança de backend só
   pega com restart.
4. Validar item a item (curl/tela) e marcar como APLICADO aqui.

## Fila

### E1 — Vitrine pública do catálogo de planos
- **Pedido (dono):** página /planos pública e coerente para campanha de Google
  Ads (entrada trial-first, 12/06/2026).
- **Onde:** `backend/src/commercial-plans` (controller).
- **Mudança:** expor um `GET` SEM auth (ex.: `/commercial-plans/public-catalog`)
  devolvendo só a vitrine: key, title, monthlyPrice, headline, badge,
  trialDays, features (catálogo `buildCommercialPlansCatalog()` sem dados de
  empresa/usuário). O front (/planos) já consome o formato `{ plans: [...] }`
  e hoje só mostra preço para visitante logado.
- **Risco/observação:** dados já são de marketing público; nenhum dado de
  tenant na resposta. Rate-limit básico recomendado.
- **Status:** APLICADO (12/06/2026) — `GET /commercial-plans/public-catalog`
  (controller público + throttle 30/min); /planos consome com fallback no
  catálogo autenticado. Validado: visitante deslogado vê R$ 45/R$ 99 reais.

### E2 — E-mail de vendedor vazando para cadastro de cliente
- **Pedido (dono):** cadastro novo de cliente recebeu o e-mail "bem-vindo à
  equipe comercial" com senha temporária — era exclusivo dos vendedores.
- **Investigação (12/06/2026):**
  - O destinatário (glaucoreserva91@gmail.com) é ADMIN da empresa nova
    "HBX3" — cadastro de CLIENTE, não vendedor.
  - Links do e-mail apontam para `https://www.hbxsystem.com.br` → quem
    enviou foi o backend de PRODUÇÃO (VPS, build antigo), não o local.
  - No código atual, o único despachante do template `seller_welcome` é a
    ativação de onboarding de vendedor (senha `Hbx@...`); o prefixo
    `Tmp@...` vem do reset de senha do master (build antigo enviava o
    welcome de vendedor nesses fluxos).
- **Mudança aplicada (local):** trava estrutural em
  `sendWelcomeAccessEmail` — `seller_welcome` NUNCA é enviado para role
  diferente de USER (loga warning e não envia). Cliente recebe apenas a
  confirmação de e-mail/trial.
- **Status:** APLICADO no local (12/06/2026). **PENDENTE: deploy do backend
  atualizado no VPS** — até lá, produção continua com o build antigo que
  vaza o e-mail. Deploy é ação do dono (INFRA.md).

<!-- Modelo de item:
### E1 — [título curto]
- **Pedido (dono):** [o que ele falou, literal]
- **Onde:** [arquivo/serviço/endpoint]
- **Mudança:** [descrição objetiva da edição]
- **Risco/observação:** [se houver]
- **Status:** pendente | APLICADO (data)
-->

### E3 — Produto nasce com preço 0 quando criado com `price`
- **Onde:** `products.service.ts` → `normalizePriceCents` + `hasOwn`.
- **Causa:** o ValidationPipe entrega o DTO com `priceCents` como propriedade
  própria `undefined`; o `hasOwn(dto,'priceCents')` é true e ignora o `price`
  enviado → produto criado com 0. Workaround atual: enviar `priceCents`.
- **Mudança:** tratar `undefined` como ausente (checar `!== undefined` em vez
  de `hasOwn`) nos normalizadores de preço/desconto/comissão.
- **Status:** APLICADO (12/06/2026 — criar com price:77 → R$77; testes 11/11 verdes)

### E4 — PATCH /products/:id retorna 500 (versionamento)
- **Onde:** `products.service.ts:268` → `this.pv.createVersion(...)`.
- **Causa:** Prisma "Expected String, provided Object" — o snapshot do
  produto vai como objeto para um campo String do ProductVersion (falta
  `JSON.stringify`).
- **Mudança:** serializar o snapshot no createVersion.
- **Status:** APLICADO (12/06/2026 — PATCH/arquivar 200; edição validada na tela do gerencial)

### E5 — Caso de cancelamento (reversão de comissão SÓ com decisão do master)
- **Pedido (dono, 12/06/2026):** todo cancelamento avisa master E vendedor;
  comissão nunca estorna sozinha; com saldo a restituir, o master triagem
  primeiro e o vendedor só é avisado no desfecho; master pode manter,
  estornar ou desvincular a venda do vendedor.
- **Desenho aprovado ("caso de cancelamento"):**
  1. Cancelamento (saleStatus → canceled/inactive) ABRE UM CASO com campo
     calculado `saldoARestituir` (anual: meses não usados; mensal: 0).
  2. **Remover o estorno automático atual** (`endsSale → commissionStatus
     'canceled'` em buildSaleCommissionPatch): no cancelamento, comissões
     recorrentes futuras desligam e o pendente/payable fica TRAVADO pelo
     caso aberto (gerador de payout ignora leads com caso aberto). `paid`
     fica pago (estorno de payout segue manual).
  3. Avisos: master SEMPRE e na hora (master-notice; fila filtrável por
     saldoARestituir > 0). Vendedor: saldo 0 → avisado no mesmo evento
     (texto FACTUAL: "cliente cancelou; futuras param; pendente em
     revisão"); saldo > 0 → avisado só quando o master finalizar.
  4. Decisão do master fecha o caso: manter (libera payable), estornar
     (canceled), desvincular venda do vendedor (verificar se o PATCH do
     gerencial cobre; senão, incluir ação própria). Desfecho gera o
     segundo aviso ao vendedor ("mantida"/"estornada").
- **Onde:** vendas.service (buildSaleCommissionPatch + geração de payout),
  modelo novo do caso (ou campos no lead), hooks de notice, ações gerencial.
- **Risco/observação:** maior item da fila — tocar com testes dos fluxos de
  comissão existentes; nenhuma mudança nos status atuais da comissão.
- **Status:** APLICADO (12/06/2026). Validado E2E ponta a ponta:
  cancelamento manteve a comissão (pending, sem estorno), abriu o caso
  (refund 0 no mensal), avisou o sino do admin ("Cancelamento: Padaria do
  Teste"), travou o payout, e a decisão "Manter" pela tela do Gerencial
  resolveu o caso preservando a comissão. Testes de comissão 68/68 verdes
  antes e depois. Implementação: colunas via schema-runtime-ensure
  (vendas-cancellation-case-columns), buildSaleCommissionPatch sem estorno
  automático, notice audiência customer, timeline factual ao vendedor,
  GET /vendas/cancellation-cases + PATCH /vendas/lead/:id/cancellation-case
  (kept|reversed|unlinked), e a seção "Casos de cancelamento — sua triagem"
  na aba Comissões do /gerencial com o filtro "só com saldo a restituir".

## Já aplicado fora da fila (referência)

- 12/06/2026 — Login com identificador caixa-insensível
  (`findByLoginIdentifier` em users.service; aplicado em login, passport e
  conflito de cadastro; senha estrita). Aprovado e validado no backend real
  antes deste combinado existir.
