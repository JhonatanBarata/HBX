# Handoff — retomar em chat limpo (12/06/2026)

> Onde paramos. As pendências detalhadas estão em `PENDENCIAS-FRONTEND-12062026.md`
> (o dono vai rodar essas em PARALELO em outro chat — não precisa atacar aqui).

## Ambiente (NÃO reiniciar à toa)
- **Frontend JÁ ESTÁ NO AR no `localhost:3001`** (preview server reaproveitável — use `preview_start name="frontend"`, ele reusa). NÃO derrube/suba sem motivo.
- Backend + db + engine no docker (`docker compose up -d db backend hbx-scraping-engine`). O **backend cai às vezes** quando o dono edita backend — reiniciar (`npm run backend:restart`) quando o front der erro de proxy (`ECONNREFUSED localhost:3000`).
- Login do produto: o dono loga sozinho (ele NÃO quer que você digite senha em campo). Conta admin da HBX = `jhonatan@hbxsystem.com.br` (id 36, empresa HBX id 2). Master de dev = `Jhonatan` / `master4961` (via API).
- Banco limpo: só sobraram **HBX (id 2)** + infra WhatsApp (id 1). Resto era teste, apagado.

## Deadline
- **Segunda 15/06/2026** entram **2 VENDEDORAS** (1 confirmou). Canal = **TELEFONE** (não WhatsApp/e-mail).
- Caminho crítico: vendedora **loga → vê a carteira dela → telefone na cara → marca resultado da ligação → agenda retorno**. Tira Webwhats/e-mail do caminho.

## O que esta sessão mexeu no FRONT (não commitado ainda)
- Band-aids: `key={v.id ?? v.name ?? i}` (dashboard) e guarda no `Av` (avatar com name undefined). A dashboard já teve a raiz corrigida por outro agente (tipagem do seller-audit).
- Botão **Excluir** membro ligado em `/configuracoes` (chama `DELETE /users/:id/delete`).
- Criado **`ConfirmDialog`** no kit (`components/hbx/shell.tsx`) — substitui `window.confirm`. Já usado no Excluir.
- ⚠️ Outro agente também edita o front em paralelo — cuidado com conflito; não editar arquivo de front que ele esteja mexendo.

## Próximo passo concreto
1. Criar as 2 vendedoras pelo **`/gerencial`** (cadastro completo). `/gerencial` ainda NÃO está no menu — abrir digitando a URL.
2. Verificar o caminho da vendedora (login → carteira → telefone → marcar ligação) e consertar só os buracos.

## Backend desta sessão (commits LOCAIS na master, nada pushado)
Motor com fetch real + MX, Encomenda Noturna (`NightOrder`), Banco de Leads, Caça de e-mail (`/night-factory/email-leads`), HBX Owner web (`hbx-owner/local-agent`, porta 3107). Doc: `docs/PLANEJAMENTOS/PR12062026002/MOTOR-DIFERENCIAL-NOTURNO.md`.
