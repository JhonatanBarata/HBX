# PLAN14062026001 — Fila de edições de BACKEND (aplicar em lote no `docker restart`)

> Doc persistente. Backend edita em lote: o código vai pro repo, mas **só vale em produção
> após `docker restart` na VPS** (o DONO roda o deploy). Frontend edita na hora (não entra aqui).

---

## FILA (14/06)

### ⏳ E-RADAR-WA — Radar usa o motor de WhatsApp COMPARTILHADO (com fallback)
Backend novo, aguardando `docker restart`:
- `radar-core-presentation.mixin.ts`: verificação de WhatsApp do Radar (lista +
  enriquecimento de card) agora passa pelo MESMO motor do Vendas — empresa-infra
  `hbx-master-whatsapp-engine` (chip do HBX, conectado pelo master via fallback).
  Helpers novos `resolveRadarWhatsappEngineCompanyId` + `radarCheckWhatsappNumbers`
  (tenta o motor compartilhado; se indisponível, **fallback** pro WhatsApp da própria
  empresa). Gate de plano (`canUseRadarWebwhatsCheck`) e fonte do `confirmed` (Webwhats,
  MOTOR.md) intactos — só muda QUAL chip faz o lookup.
- Sem migration. Build do backend já verde no repo. **Depende:** o chip HBX
  (19 92012-1720) precisa estar conectado na empresa-motor (master conecta via `/master`).

### ⏳ E-S2 — Sprint 2 do PR14062026012 (HBX Full + alerta master)
Backend novo, aguardando `docker restart`:
- `commercial-plan-catalog.ts`: preço Full **149,90 → 349,90**.
- Novo `MasterAlertModule`/`MasterAlertService` + `POST /commercial-plans/request-full`.
- Pra o WhatsApp do alerta disparar, setar no `.env` da VPS: `MASTER_ALERT_WA_COMPANY_ID`
  (empresa HBX com WhatsApp conectado) e, se quiser sobrescrever, `MASTER_ALERT_EMAIL` /
  `MASTER_ALERT_WHATSAPP_TO`. Build + prisma validate + catalog test já verdes no repo.

---

Os itens antigos (E14 boas-vindas/tutorial, E1 exclusão de empresa órfã, E2 inbox sem 503)
**foram aplicados em produção** e saíram daqui.

### Dependência conhecida (entra aqui quando construída)
- **B2 / score do lead** (card "Radar B2") — quando criar a coluna
  `VendasLead.opportunityScore` (runtime-ensure em `prisma.service.ts`), o `docker restart`
  dela entra nesta fila. Hoje é trabalho de card, não item de restart.
