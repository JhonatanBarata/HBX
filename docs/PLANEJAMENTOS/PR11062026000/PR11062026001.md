# PR11062026001 — Log de relink da sprint noturna (11/06/2026, madrugada)

Status: EM EXECUÇÃO — sprint autônoma do PR10062026010 enquanto o dono dorme.
Regra combinada: tudo que precisar ser "linkado" de novo com o dono fica registrado
AQUI, item por item. Amanhã linkamos juntos.

---

## ⚠️ PENDÊNCIAS DE LINK (para a sessão de amanhã)

### L1 — ~~IMAGEM NÃO RECEBIDA~~ → RESOLVIDO (~04h)
Errei o alvo: a fonte visual sempre foi a pasta `docs/TEMAS` (handoff corporativo) —
não precisava de imagem. As telas da sprint tinham nascido com as peças `HbxAdminUi`
(visual admin, errado). Corrigido em **ef5c0885**: shell corporativo real (sidebar +
topbar do handoff), boasvindas/planos/pre-checkout compostos de
`HbxCorporateKpis/Panel/Tag` + `tealButton/ghostButton`. Contratos de backend
intocados; E2E 3 personas verde de novo após a correção.

### L2 — Validação visual do dono
Salvaguarda do PR-003 mantida: cada tela nova precisa de validação visual sua contra o
HTML de referência do handoff. As telas construídas nesta sprint estão funcionais e no
kit, mas **nenhuma foi validada visualmente por você ainda**. Conferir uma a uma.

### L3 — E2E das 3 personas: ✅ GUARDIÃO VERDE (rodado ~03h contra o build novo)
`npx playwright test tests/e2e/fluxo-contratante-vendedor.spec.ts`:
- ✅ "vendedor opera sem 403 e sem ver cobrança em nenhum estado da empresa" (12.2s)
- ✅ "master cria empresa → convite → contratante define senha e cai no checkout
  pendente" (11.1s)
Esses dois fluxos exercitam exatamente o que foi reconstruído: boasvindas nova (senha
do primeiro acesso), pre-checkout novo, PreCheckoutGate com audiência absorvida.

Suíte completa (`npx playwright test`): 4 passed, 3 failed, 7 skipped.
**As 3 falhas são todas [mobile-chromium] em superfícies NÃO tocadas nesta sprint**
(análise em L7) — confirmar amanhã que já falhavam antes, rodando contra 43b007bd
se quiser a prova formal.

### L7 — 3 falhas de E2E mobile (pré-existentes à sprint, com evidência)
1. `whatsapp-mobile.spec.ts` — testa `/whatsapp?focus=qr` esperando "Vincular por
   telefone". O redirect `/whatsapp → /atendimento/automacao?tab=connection` veio do
   SEU commit 43b007bd (eu só deletei arquivos que já estavam fora de rota). O texto
   vive em `WhatsAppOperationalDialog.tsx` (componente vivo) — **verificar juntos se a
   automação renderiza o vínculo por telefone do trial com `focus=qr`, ou se o spec
   precisa ser reescrito para a rota nova**. Se a automação NÃO tem o fluxo, é perda
   funcional do redirect (anterior à sprint) que precisa de correção.
2. `login-register-mobile.spec.ts` — cadastro mobile esperando "List pago, Lead trial
   e plano empresarial". Register não foi tocado na sprint; suspeita: taxonomia do
   catálogo pós-DROP do backend (ontem). Conferir contra o catálogo da API.
3. `vendas-observacao.spec.ts` — /vendas mobile (tela não tocada; entra só em R2.10).
   Pode ser dependência de dados (board precisa de card com observação).

### L8 — Estado do servidor local (mexi de propósito, com evidência)
O dev server da porta 3001 (node PID 15416, iniciado 01:18) estava devolvendo **500 em
TODAS as rotas** (até /login e /app2, não tocadas) — estado HMR podre, não era o
código: `npm run build` do mesmo código passou inteiro. Eu o derrubei e subi
`npm run start` (produção, build validado) na 3001 para rodar o E2E.
**Amanhã: derrubar o `npm run start` antes de voltar pro `npm run dev`.**

### L4 — Decisões abertas do PR-010 (não travaram a sprint, mas precisam de dono)
- `auto-replies` e `messages`: viram painéis (automação/master) ou morrem com endpoint
  em R4?
- `night-factory`: ferramenta viva (vira satélite do master) ou morta?
- "financeiro do contratante": tratei como a própria `/pagamento` (alias
  `/dashboard/financeiro`) — confirmar.
- `/checkout`: confirmar se é página ativa do fluxo `/financeiro/checkout` ou casca.

### L5 — Re-homing do TopBar (gaps propositais)
O `HbxAppShell` novo absorveu do TopBar: identidade (perfil/logout), avisos do master
(`/vendas/master-notices` + ack via `HbxPersistentNotice`), chip de contexto master
(`/master-context/exit`), tema claro/escuro. **Ficou no TopBar legado de propósito**
(telas velhas dependem até R3, e o shell novo recebe quando a tela dona chegar):
- fluxo completo de conexão WhatsApp (modal QR/start/status/disconnect) — a lógica já
  vive em `lib/whatsapp-connection-flow.ts`; o overlay do shell entra com a tela de
  Atendimento (R2.9);
- badges de inbox/recovery (sinos) — entram com Atendimento;
- indicador de runs do radar + cancelar — entra com Radar (R2.8);
- busca ⌘K — não implementada (decidir juntos se entra);
- animação de transição no logout (`runGlobalShutdown`) — logout do shell novo é
  direto, sem a animação; decidir se ela volta.
- **Nav do shell novo reusa `ModuleNav compact`** (lógica de visibilidade por persona
  intacta) dentro do chrome corporativo — o visual da sidebar ainda não é o do handoff;
  restilizar juntos (só CSS, sem mexer em lógica).

### L6 — R1.3 (tema na API) adiado de propósito
O ThemeProvider tem escopos company/system/user (`/profile/theme-preferences` com
PATCH por escopo). Integrar o modo corporativo nisso de madrugada arriscava corromper
configs de tema existentes. O modo corporativo persiste em localStorage
(`hbx:corporate-mode`) por enquanto. Fazer a integração juntos.

---

## Diário da sprint (preenchido conforme executo)

| Hora | Fase | Commit | Resultado |
|---|---|---|---|
| ~01h | Passo 0 | be133991 | docs PR-010 + renames + este log |
| ~01h | R1.1 | e7764dca | lint do shell: 0 erros (useSyncExternalStore) |
| ~02h | R1.2 | f7c12d31 | HbxAppShell oficial (identidade, nav, avisos, contexto master) |
| ~02h | R2.1 | 9ea726c3 | pre-checkout puro no kit; audiência no PreCheckoutGate; CSS local morto |
| ~02h | R2.5 | 65d5eecd | /whatsapp: cadáver de 709 linhas + wizard deletados (já era redirect) |
| ~02h | R2.2 | 5aca8a81 | boasvindas = Home do kit; D.4 preservado; CSS local morto |
| ~03h | R2.3a | c8ee2587 | /planos no kit; FALLBACK_PLANS (preço hardcoded) MORTO; trial em HbxModal |
| ~03h | gates | — | lint 0 erros; tsc verde; `npm run build` verde (todas as rotas) |
| ~03h | E2E | — | 3 personas VERDE; suíte 4✅/3❌(pré-existentes, L7)/7 skip |
| ~04h | visual | ef5c0885 | correção: visual corporativo do handoff nas 3 telas + shell; E2E personas verde de novo; servidor 3001 atualizado |

## O que ficou de fora desta noite, de propósito (além das telas grandes)
- **R2.3b `/pagamento`** (1.890 linhas + page.mobile.client): máquina de checkout do
  Mercado Pago — reescrever sem você validar ao vivo era o maior risco de retrabalho
  da noite. Os 2 warnings de lint `isBillingGraceActive` morrem com ela.
- **R2.4 `/tutorial`** (3.263 linhas): grande e dependente de validação visual.
- **R1.3 tema na API** (L6) e **imagem não recebida** (L1).

---

## O que NÃO fiz de propósito (risco de retrabalho alto sem você)

- `vendas` (12.870 linhas), `atendimento` (11.551), `gerencial` (4.455),
  `master command center`, `radar-digital` (5.920): são as telas de maior risco do
  PR-010 (R2.6–R2.10) — refazer de madrugada sem sua validação geraria retrabalho
  quase certo. Ficam para as sessões conjuntas.
- Demolição R3 (TopBar, DashboardScaffold, HbxPopup, aliases): só depois de R2
  completo, por definição do plano.
- Backend R4: intocado nesta sprint (princípio P2 — backend é contrato).
