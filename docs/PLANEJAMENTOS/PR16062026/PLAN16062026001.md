# PLAN16062026001 — Fila de edições de BACKEND (aplicar em lote no `docker restart`)

> Doc **PERSISTENTE** (regra CLAUDE.md): migra a cada virada de dia, só esvazia quando os itens
> são aplicados. Backend edita em lote: o código vai pro repo, mas **só vale em produção após
> `docker restart` na VPS** (o DONO roda o deploy). Frontend edita na hora (não entra aqui).

---

## FILA (16/06)

### ⏳ SIGNUP-NAME — saudação do e-mail de cadastro usa o NOME (não o e-mail)
(de `PR15062026-BACKEND001`) **Sintoma:** o e-mail de signup abre com `olá {email}` (o endereço).
**Correção mínima (só o render do template, NÃO mexer no fluxo):** a saudação usa o campo
"Como deseja ser chamado?" (`name` do `/auth/signup`), não o `email`. Arquivos prováveis:
`backend/src/auth` (signup), módulo de e-mail por empresa / Master mailer, templates
`email_confirmation` / `seller_welcome`.
**Futuro (anotado, NÃO agora):** "Como deseja ser chamado?" pode virar o login.

### ⏳ E-RADAR-WA — Radar usa o motor de WhatsApp COMPARTILHADO (com fallback)
(de `PLAN14062026001`) `radar-core-presentation.mixin.ts`: a verificação de WhatsApp do Radar
(lista + enriquecimento de card) passa pelo MESMO motor do Vendas — empresa-infra
`hbx-master-whatsapp-engine` (chip do HBX, master conecta via fallback). Helpers novos
`resolveRadarWhatsappEngineCompanyId` + `radarCheckWhatsappNumbers` (tenta o motor compartilhado;
se indisponível, **fallback** pro WhatsApp da própria empresa). Gate de plano e fonte do
`confirmed` (Webwhats) intactos. **Sem migration.** Build verde no repo. **Depende:** o chip HBX
(19 92012-1720) conectado na empresa-motor (master conecta via `/master`).

---

## Superado / saiu da fila
- **E-S2** (preço Full 349,90) — **SUPERADO** pelos preços de 15/06; agora vive no bloco
  `PR16062026010` (Company a partir de 445,90; criar `hbx_pro`).
- **B2 / `VendasLead.opportunityScore`** — aplicado (saiu da fila).
- Itens antigos E14 (boas-vindas/tutorial), E1 (empresa órfã), E2 (inbox 503) — aplicados em prod.

## Status
2 itens vivos (SIGNUP-NAME, E-RADAR-WA); aguardam `docker restart` com "go".
